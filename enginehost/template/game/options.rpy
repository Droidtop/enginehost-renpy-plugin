define config.name = "enginehost Ren'Py runtime"
define config.version = "0.1.0"

init python:
    build.name = "enginehost-renpy-runtime"
    build.version = "0.1.0"
    build.classify("**", "android")
